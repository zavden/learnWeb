---
entry: src/main.js
framework: vue
mode: multi-file
---

## @file src/main.js
## @lang javascript
## @role entry

```javascript
import { createApp } from 'vue';
import App from './App.vue';
import './styles.css';

createApp(App).mount('#app');
```

## @file src/App.vue
## @lang vue
## @role app

```vue
<template>
  <OverviewPage />
</template>

<script setup>
import OverviewPage from './pages/OverviewPage.vue';
</script>
```

## @file src/pages/OverviewPage.vue
## @lang vue
## @role page

```vue
<template>
  <main class="overview-shell">
    <h1>Page + component</h1>
    <p>A page SFC can group smaller component files into one lesson screen.</p>
    <div class="overview-grid">
      <StatCard title="Entry" detail="main.js mounts the whole project." />
      <StatCard title="Page" detail="OverviewPage.vue owns the screen layout." />
      <StatCard title="Component" detail="StatCard.vue keeps the UI repeatable." />
    </div>
  </main>
</template>

<script setup>
import StatCard from '../components/StatCard.vue';
</script>
```

## @file src/components/StatCard.vue
## @lang vue
## @role component

```vue
<template>
  <article class="stat-card">
    <strong>{{ title }}</strong>
    <p>{{ detail }}</p>
  </article>
</template>

<script setup>
defineProps({
  detail: {
    type: String,
    required: true,
  },
  title: {
    type: String,
    required: true,
  },
});
</script>

<style scoped>
.stat-card {
  display: grid;
  gap: 8px;
  padding: 16px;
  border-radius: 16px;
  background: rgba(255, 255, 255, 0.88);
}

.stat-card p {
  margin: 0;
}
</style>
```

## @file src/styles.css
## @lang css
## @role style

```css
body {
  margin: 0;
  background: #eef2ff;
  color: #312e81;
  font-family: system-ui, sans-serif;
}

.overview-shell {
  display: grid;
  gap: 14px;
  max-width: 680px;
  margin: 0 auto;
}

.overview-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 12px;
}
```
