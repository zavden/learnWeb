---
entry: src/main.js
framework: vue
mode: multi-file
---

## @file src/main.js
## @lang javascript
## @role entry

```javascript
import { createApp } from 'vue';
import App from './App.vue';
import './styles.css';

createApp(App).mount('#app');
```

## @file src/App.vue
## @lang vue
## @role app

```vue
<template>
  <AgendaPage />
</template>

<script setup>
import AgendaPage from './pages/AgendaPage.vue';
</script>
```

## @file src/pages/AgendaPage.vue
## @lang vue
## @role page

```vue
<template>
  <main class="agenda-shell">
    <h1>Page + store + util</h1>
    <SessionTile
      v-for="session in sessions"
      :key="session.id"
      :length-label="formatSessionLength(session.minutes)"
      :title="session.title"
    />
  </main>
</template>

<script setup>
import SessionTile from '../components/SessionTile.vue';
import { useAgendaStore } from '../stores/useAgendaStore.js';
import { formatSessionLength } from '../utils/formatSessionLength.js';

const { sessions } = useAgendaStore();
</script>
```

## @file src/components/SessionTile.vue
## @lang vue
## @role component

```vue
<template>
  <article class="session-tile">
    <strong>{{ title }}</strong>
    <span>{{ lengthLabel }}</span>
  </article>
</template>

<script setup>
defineProps({
  lengthLabel: {
    type: String,
    required: true,
  },
  title: {
    type: String,
    required: true,
  },
});
</script>

<style scoped>
.session-tile {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  padding: 14px 16px;
  border-radius: 16px;
  background: rgba(255, 255, 255, 0.88);
}
</style>
```

## @file src/stores/useAgendaStore.js
## @lang javascript
## @role context

```javascript
const sessions = [
  { id: 1, title: 'Entry + mount', minutes: 15 },
  { id: 2, title: 'First page component', minutes: 25 },
  { id: 3, title: 'Extract utility helpers', minutes: 10 },
];

export function useAgendaStore() {
  return {
    sessions,
  };
}
```

## @file src/utils/formatSessionLength.js
## @lang javascript
## @role util

```javascript
export function formatSessionLength(minutes) {
  return `${minutes} min`;
}
```

## @file src/styles.css
## @lang css
## @role style

```css
body {
  margin: 0;
  background: #fffbeb;
  color: #92400e;
  font-family: system-ui, sans-serif;
}

.agenda-shell {
  display: grid;
  gap: 12px;
  max-width: 620px;
  margin: 0 auto;
}
```
