---
framework: vue
mode: multi-file
entry: src/main.ts
---

## @file src/main.ts
## @lang typescript
## @role entry

```typescript
import { createApp } from 'vue';
import App from './App.vue';
import './styles.scss';

createApp(App).mount('#app');
```

## @file src/App.vue
## @lang vue
## @role app

```vue
<template>
  <main class="tsx-shell">
    <h1>{{ title }}</h1>
    <MetricCard label="Typed lessons" :value="count" />
    <button type="button" @click="increment">Increment</button>
  </main>
</template>

<script setup lang="ts">
import { ref } from 'vue';
import MetricCard from './components/MetricCard.vue';

const title = 'Vue SFC TypeScript';
const count = ref<number>(2);
const increment = () => {
  count.value += 1;
};
</script>
```

## @file src/components/MetricCard.vue
## @lang vue
## @role component

```vue
<template>
  <section class="metric-card">
    <span>{{ label }}</span>
    <strong>{{ value }}</strong>
  </section>
</template>

<script setup lang="ts">
defineProps<{
  label: string;
  value: number;
}>();
</script>

<style scoped>
.metric-card {
  display: grid;
  gap: 8px;
  padding: 16px;
  border-radius: 16px;
  background: rgba(255, 255, 255, 0.08);
}

.metric-card strong {
  font-size: 32px;
  color: #38bdf8;
}
</style>
```

## @file src/styles.scss
## @lang scss
## @role style

```scss
body {
  margin: 0;
  background: #0f172a;
  color: #e2e8f0;
  font-family: system-ui, sans-serif;
}

.tsx-shell {
  display: grid;
  gap: 16px;
  max-width: 460px;
  margin: 0 auto;

  button {
    justify-self: start;
    border: 0;
    border-radius: 999px;
    padding: 10px 16px;
    background: #38bdf8;
    color: #0f172a;
    cursor: pointer;
    font-weight: 700;
  }
}
```
