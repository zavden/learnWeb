---
framework: vue
mode: multi-file
entry: src/main.js
---

## @file src/main.js
## @lang javascript
## @role entry

```javascript
import { createApp } from 'vue';
import App from './App.vue';
import './styles.css';

createApp(App).mount('#app');
```

## @file src/App.vue
## @lang vue
## @role app

```vue
<template>
  <main class="json-shell">
    <h1>{{ dataset.title }}</h1>
    <ul>
      <li v-for="module in dataset.modules" :key="module.id">
        {{ module.label }} - {{ module.steps }} steps
      </li>
    </ul>
  </main>
</template>

<script setup>
import dataset from './data/modules.json';
</script>
```

## @file src/data/modules.json
## @lang json
## @role config

```json
{
  "title": "Vue JSON overview",
  "modules": [
    { "id": 1, "label": "Refs", "steps": 3 },
    { "id": 2, "label": "Computed", "steps": 2 },
    { "id": 3, "label": "Composables", "steps": 4 }
  ]
}
```

## @file src/styles.css
## @lang css
## @role style

```css
body {
  margin: 0;
  background: #f8fafc;
  color: #0f172a;
  font-family: system-ui, sans-serif;
}

.json-shell {
  display: grid;
  gap: 12px;
  max-width: 520px;
  margin: 0 auto;
}
```
