# HTML

```html
<label class="field">
  Search
  <input id="search" type="text" placeholder="Type a keyword" />
</label>
<p id="mirror" class="mirror">Start typing...</p>
```

# CSS

```css
body {
  margin: 0;
  padding: 32px;
  background: #fff7ed;
  color: #7c2d12;
  font-family: system-ui, sans-serif;
}

.field {
  display: grid;
  gap: 8px;
  max-width: 320px;
}

input {
  padding: 10px 12px;
  border: 1px solid #fdba74;
  border-radius: 12px;
}

.mirror {
  margin-top: 16px;
  font-weight: 600;
}
```

# JavaScript

```javascript
const input = document.getElementById('search');
const mirror = document.getElementById('mirror');

input?.addEventListener('input', () => {
  mirror.textContent = input.value ? `Searching for: ${input.value}` : 'Start typing...';
});
```
