# SVG

```svg
<svg viewBox="0 0 240 120" xmlns="http://www.w3.org/2000/svg">
  <line id="bar-line" class="bar-line" x1="30" y1="60" x2="210" y2="60" />
</svg>
```

# CSS

```css
body {
  margin: 0;
  min-height: 100vh;
  display: grid;
  place-items: center;
  background: #111827;
}

.bar-line {
  stroke: #22c55e;
  stroke-width: 14;
  stroke-linecap: round;
  transition: stroke-width 160ms ease;
}
```

# JavaScript

```javascript
const line = document.getElementById('bar-line');

line?.addEventListener('click', () => {
  const thick = line.style.strokeWidth === '24';
  line.style.strokeWidth = thick ? '14' : '24';
});
```
