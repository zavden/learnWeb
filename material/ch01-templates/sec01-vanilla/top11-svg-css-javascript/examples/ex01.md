---
console: true
---

# SVG

```svg
<svg viewBox="0 0 180 120" xmlns="http://www.w3.org/2000/svg">
  <circle id="pulse-dot" class="pulse-dot" cx="90" cy="60" r="30" />
</svg>
```

# CSS

```css
body {
  margin: 0;
  min-height: 100vh;
  display: grid;
  place-items: center;
  background: #020617;
}

.pulse-dot {
  fill: #38bdf8;
  transition: transform 160ms ease, fill 160ms ease;
  cursor: pointer;
}

.pulse-dot.active {
  fill: #f97316;
  transform: scale(1.15);
}
```

# JavaScript

```javascript
const dot = document.getElementById('pulse-dot');

dot?.addEventListener('click', () => {
  dot.classList.toggle('active');
  console.log('pulse-dot active', dot.classList.contains('active'));
});
```
