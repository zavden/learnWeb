# HTML

```html
<section class="card">
  <p class="eyebrow">HTML + CSS</p>
  <h1>Profile Card</h1>
  <p>Simple styling around semantic markup.</p>
</section>
```

# CSS

```css
body {
  margin: 0;
  min-height: 100vh;
  display: grid;
  place-items: center;
  background: linear-gradient(180deg, #eff6ff, #dbeafe);
  font-family: system-ui, sans-serif;
}

.card {
  display: grid;
  gap: 10px;
  max-width: 360px;
  padding: 24px;
  border-radius: 20px;
  background: white;
  color: #1e3a8a;
}

.card h1,
.card p {
  margin: 0;
}

.eyebrow {
  font-size: 12px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}
```
