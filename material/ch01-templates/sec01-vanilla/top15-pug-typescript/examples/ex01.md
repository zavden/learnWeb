# Pug

```pug
button#hello-btn(type="button") Press me
```

# TypeScript

```typescript
const button = document.getElementById('hello-btn') as HTMLButtonElement | null;

button?.addEventListener('click', () => {
  button.textContent = 'Pressed';
});
```
