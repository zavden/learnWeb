---
framework: react
---

# JSX
```jsx
function App() {
  const lessons = ['Props', 'State', 'Effects', 'Context', 'Reducers'];
  const [query, setQuery] = React.useState('');

  const filtered = lessons.filter((lesson) => (
    lesson.toLowerCase().includes(query.toLowerCase())
  ));

  return (
    <main>
      <h1>Filter lessons</h1>
      <input
        type="text"
        value={query}
        placeholder="Search lessons"
        onChange={(event) => setQuery(event.target.value)}
      />
      <ul>
        {filtered.map((lesson) => <li key={lesson}>{lesson}</li>)}
      </ul>
    </main>
  );
}
```
