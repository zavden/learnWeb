---
entry: src/main.tsx
framework: react
mode: multi-file
---

## @file src/main.tsx
## @lang tsx
## @role entry

```tsx
import React from 'react';
import { createRoot } from 'react-dom/client';
import { App } from './App.tsx';
import './styles.scss';

createRoot(document.getElementById('root') as HTMLElement).render(<App />);
```

## @file src/App.tsx
## @lang tsx
## @role app

```tsx
import { PreferencesProvider } from './context/PreferencesContext.tsx';
import { PreferencesPanel } from './components/PreferencesPanel.tsx';

export function App() {
  return (
    <PreferencesProvider>
      <main className="screen">
        <p className="eyebrow">Project TSX + SCSS</p>
        <h1>Context + component</h1>
        <PreferencesPanel />
      </main>
    </PreferencesProvider>
  );
}
```

## @file src/components/PreferencesPanel.tsx
## @lang tsx
## @role component

```tsx
import { usePreferences } from '../context/PreferencesContext.tsx';

export function PreferencesPanel() {
  const { compactMode, toggleCompactMode } = usePreferences();

  return (
    <section className="panel">
      <h2>Compact mode: {compactMode ? 'On' : 'Off'}</h2>
      <button type="button" onClick={toggleCompactMode}>Toggle preference</button>
    </section>
  );
}
```

## @file src/context/PreferencesContext.tsx
## @lang tsx
## @role context

```tsx
import { createContext, useContext, useState, type ReactNode } from 'react';

type PreferencesValue = {
  compactMode: boolean;
  toggleCompactMode: () => void;
};

const PreferencesContext = createContext<PreferencesValue>({
  compactMode: false,
  toggleCompactMode: () => {},
});

export function PreferencesProvider({ children }: { children: ReactNode }) {
  const [compactMode, setCompactMode] = useState<boolean>(false);

  return (
    <PreferencesContext.Provider
      value={{
        compactMode,
        toggleCompactMode: () => setCompactMode((value) => !value),
      }}
    >
      {children}
    </PreferencesContext.Provider>
  );
}

export function usePreferences() {
  return useContext(PreferencesContext);
}
```

## @file src/styles.scss
## @lang scss
## @role style

```scss
$accent: #16a34a;

body {
  margin: 0;
  background: #f0fdf4;
  color: #14532d;
  font-family: system-ui, sans-serif;
}

.screen {
  display: grid;
  gap: 16px;
  max-width: 560px;
  margin: 0 auto;
}

.eyebrow {
  margin: 0;
  font-size: 12px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: $accent;
}

.panel {
  display: grid;
  gap: 12px;
  padding: 20px;
  border-radius: 20px;
  background: white;
}

.panel button {
  justify-self: start;
  border: 0;
  border-radius: 999px;
  padding: 10px 16px;
  background: $accent;
  color: #ffffff;
  cursor: pointer;
}
```
