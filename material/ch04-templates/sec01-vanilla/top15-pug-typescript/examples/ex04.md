# Pug

```pug
ul#todo-list
  li Review parser
  li Open gallery
```

# TypeScript

```typescript
const list = document.getElementById('todo-list') as HTMLUListElement | null;
const item = document.createElement('li');

item.textContent = 'Practice typed DOM access';
list?.appendChild(item);
```
