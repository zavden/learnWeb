---
entry: src/main.js
framework: vue
mode: multi-file
---

## @file src/main.js
## @lang javascript
## @role entry

```javascript
import { createApp } from 'vue';
import App from './App.vue';
import './styles.css';

createApp(App).mount('#app');
```

## @file src/App.vue
## @lang vue
## @role app

```vue
<template>
  <main class="pill-shell">
    <h1>Component combination</h1>
    <p>A root SFC can stay very small and let child components handle the tiny UI pieces.</p>
    <div class="pill-row">
      <StatusPill label="Template" tone="blue" />
      <StatusPill label="Script setup" tone="green" />
      <StatusPill label="Scoped style" tone="amber" />
    </div>
  </main>
</template>

<script setup>
import StatusPill from './components/StatusPill.vue';
</script>
```

## @file src/components/StatusPill.vue
## @lang vue
## @role component

```vue
<template>
  <span class="status-pill" :class="`status-pill--${tone}`">{{ label }}</span>
</template>

<script setup>
defineProps({
  label: {
    type: String,
    required: true,
  },
  tone: {
    type: String,
    default: 'blue',
  },
});
</script>

<style scoped>
.status-pill {
  display: inline-flex;
  align-items: center;
  padding: 8px 14px;
  border-radius: 999px;
  font-weight: 700;
  background: rgba(255, 255, 255, 0.9);
}

.status-pill--blue {
  color: #1d4ed8;
}

.status-pill--green {
  color: #047857;
}

.status-pill--amber {
  color: #b45309;
}
</style>
```

## @file src/styles.css
## @lang css
## @role style

```css
body {
  margin: 0;
  background: #eff6ff;
  color: #1e3a8a;
  font-family: system-ui, sans-serif;
}

.pill-shell {
  display: grid;
  gap: 14px;
  max-width: 560px;
  margin: 0 auto;
}

.pill-row {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
}
```
