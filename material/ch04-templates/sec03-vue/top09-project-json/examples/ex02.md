---
framework: vue
mode: multi-file
entry: src/main.js
---

## @file src/main.js
## @lang javascript
## @role entry

```javascript
import { createApp } from 'vue';
import App from './App.js';
import './styles.css';

createApp(App).mount('#app');
```

## @file src/App.js
## @lang javascript
## @role app

```javascript
import { defineComponent } from 'vue';
import dataset from './data/cards.json';
import render from './App.html';

export default defineComponent({
  setup() {
    return {
      dataset,
    };
  },
  render,
});
```

## @file src/App.html
## @lang html
## @role markup

```html
<main class="json-cards">
  <h1>{{ dataset.title }}</h1>
  <article v-for="card in dataset.cards" :key="card.id" class="json-cards__item">
    <strong>{{ card.label }}</strong>
    <p>{{ card.note }}</p>
  </article>
</main>
```

## @file src/data/cards.json
## @lang json
## @role config

```json
{
  "title": "Render files + JSON",
  "cards": [
    { "id": 1, "label": "State", "note": "Use refs for reactive values." },
    { "id": 2, "label": "Events", "note": "Emit from child components." },
    { "id": 3, "label": "Files", "note": "Split when structure matters." }
  ]
}
```

## @file src/styles.css
## @lang css
## @role style

```css
body {
  margin: 0;
  background: #eff6ff;
  color: #1e3a8a;
  font-family: system-ui, sans-serif;
}

.json-cards {
  display: grid;
  gap: 12px;
  max-width: 560px;
  margin: 0 auto;
}

.json-cards__item {
  display: grid;
  gap: 4px;
  padding: 16px;
  border-radius: 14px;
  background: #ffffff;
}
```
