---
framework: vue
mode: multi-file
entry: src/main.js
---

## @file src/main.js
## @lang javascript
## @role entry

```javascript
import { createApp } from 'vue';
import App from './App.vue';
import './styles.css';

createApp(App).mount('#app');
```

## @file src/App.vue
## @lang vue
## @role app

```vue
<template>
  <main class="timeline-shell">
    <h1>{{ timeline.title }}</h1>
    <ul class="timeline-shell__list">
      <li v-for="item in timeline.items" :key="item.id">
        <strong>{{ item.label }}</strong>
        <span>{{ item.duration }}</span>
      </li>
    </ul>
  </main>
</template>

<script setup>
import timeline from './data/timeline.json';
</script>
```

## @file src/data/timeline.json
## @lang json
## @role config

```json
{
  "title": "Timeline from JSON",
  "items": [
    { "id": 1, "label": "Read the brief", "duration": "5 min" },
    { "id": 2, "label": "Edit the component", "duration": "12 min" },
    { "id": 3, "label": "Review the output", "duration": "8 min" }
  ]
}
```

## @file src/styles.css
## @lang css
## @role style

```css
body {
  margin: 0;
  background: #fefce8;
  color: #713f12;
  font-family: system-ui, sans-serif;
}

.timeline-shell {
  display: grid;
  gap: 12px;
  max-width: 540px;
  margin: 0 auto;
}

.timeline-shell__list {
  display: grid;
  gap: 10px;
  margin: 0;
  padding: 0;
  list-style: none;
}

.timeline-shell__list li {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  padding: 12px 14px;
  border-radius: 12px;
  background: rgba(255, 255, 255, 0.78);
}
```
