---
framework: react
---

# TSX
```tsx
function App() {
  const [count, setCount] = React.useState<number>(0);

  return (
    <main>
      <h1>Typed counter</h1>
      <button type="button" onClick={() => setCount(count + 1)}>
        Count: {count}
      </button>
    </main>
  );
}
```
