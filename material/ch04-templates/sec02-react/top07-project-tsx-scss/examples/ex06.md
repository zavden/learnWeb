---
entry: src/main.tsx
framework: react
mode: multi-file
---

## @file src/main.tsx
## @lang tsx
## @role entry

```tsx
import React from 'react';
import { createRoot } from 'react-dom/client';
import { App } from './App.tsx';
import './styles.scss';

createRoot(document.getElementById('root') as HTMLElement).render(<App />);
```

## @file src/App.tsx
## @lang tsx
## @role app

```tsx
import { LessonsPage } from './pages/LessonsPage.tsx';

export function App() {
  return <LessonsPage />;
}
```

## @file src/pages/LessonsPage.tsx
## @lang tsx
## @role page

```tsx
import { LessonTile } from '../components/LessonTile.tsx';

const lessons = [
  { id: 'entry', title: 'Entry file', detail: 'main.tsx mounts the root component.' },
  { id: 'app', title: 'App component', detail: 'App.tsx stays small and delegates layout.' },
  { id: 'page', title: 'Page component', detail: 'A page groups the screen-level UI.' },
];

export function LessonsPage() {
  return (
    <main className="screen">
      <p className="eyebrow">Project TSX + SCSS</p>
      <h1>Page + component structure</h1>
      <div className="lesson-grid">
        {lessons.map((lesson) => (
          <LessonTile key={lesson.id} title={lesson.title} detail={lesson.detail} />
        ))}
      </div>
    </main>
  );
}
```

## @file src/components/LessonTile.tsx
## @lang tsx
## @role component

```tsx
type LessonTileProps = {
  detail: string;
  title: string;
};

export function LessonTile({ detail, title }: LessonTileProps) {
  return (
    <article className="lesson-tile">
      <strong>{title}</strong>
      <p>{detail}</p>
    </article>
  );
}
```

## @file src/styles.scss
## @lang scss
## @role style

```scss
$surface: rgba(255, 255, 255, 0.88);
$accent: #2563eb;

body {
  margin: 0;
  background: #eff6ff;
  color: #1e3a8a;
  font-family: system-ui, sans-serif;
}

.screen {
  display: grid;
  gap: 16px;
  max-width: 720px;
  margin: 0 auto;
}

.eyebrow {
  margin: 0;
  font-size: 12px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: $accent;
}

.lesson-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 12px;
}

.lesson-tile {
  display: grid;
  gap: 8px;
  padding: 18px;
  border-radius: 18px;
  background: $surface;

  p {
    margin: 0;
  }
}
```
