---
renderer: shader
resolution: 670x540
example_description: "First shader baseline: a simple UV gradient driven by the built-in resolution uniform."
example_tags: "shader|glsl|webgl|gradient|resolution"
example_rating: 4
example_importance: important
---

# Vertex

```vertex
attribute vec2 a_position;
varying vec2 v_uv;

void main() {
  v_uv = a_position * 0.5 + 0.5;
  gl_Position = vec4(a_position, 0.0, 1.0);
}
```

# Fragment

```fragment
precision mediump float;

uniform vec2 u_resolution;

void main() {
  vec2 uv = gl_FragCoord.xy / u_resolution;
  vec3 color = vec3(uv.x, uv.y, 0.35 + 0.25 * (uv.x * uv.y));
  gl_FragColor = vec4(color, 1.0);
}
```
