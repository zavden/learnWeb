---
example_description: "Animated wave shader that introduces u_time with a small color rhythm."
example_importance: critical
example_rating: 5
example_tags: "shader|glsl|animation|u_time|wave"
renderer: shader
resolution: 960x540
---

# Vertex

```vertex
attribute vec2 a_position;
varying vec2 v_uv;

void main() {
  v_uv = a_position * 0.5 + 0.5;
  gl_Position = vec4(a_position, 0.0, 1.0);
}
```

# Fragment

```fragment
precision mediump float;

uniform float u_time;
uniform vec2 u_resolution;

void main() {
  vec2 uv = gl_FragCoord.xy / u_resolution;
  float wave = 0.5 + 0.5 * sin(u_time * 2.0 + uv.x * 12.0);
  float glow = 0.5 + 0.5 * cos(u_time * 1.4 + uv.y * 9.0);
  vec3 color = vec3(uv.x * 0.65 + 0.2, wave, glow);
  gl_FragColor = vec4(color, 1.0);
}
```